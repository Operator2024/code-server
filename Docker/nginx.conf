user  root;
worker_processes  2;

error_log /var/log/nginx/error.log debug;

events {
    worker_connections  64;
}

http {
    server {
        access_log /var/log/nginx/access.log combined;
        listen       80;
        server_name  %INSERT_YOUR_SERVER_NAME%;

        proxy_buffers 48 16k;
        proxy_buffer_size 16k;

        location / {
            rewrite ^/(.*)$ http://www.google.com/search?q=$1 permanent;
            #root   /usr/share/nginx/html;
            #index  index.html index.htm;
        }

        location ~* /YOURPATH($|/$) {
        rewrite ^/(.*)$ YOUR PATH permanent;
        }

        # location /test{
        #     proxy_pass http://code-server:8443/;
        #     proxy_connect_timeout 3s;
        # }

        # location / {
        #    try_files $uri $uri @code-server;
        # }

        # location @backend {
        #    proxy_pass http://code-server:8443;
        # }

    #    location ~* .(png|ico|gif|jpg|jpeg|css|js)$ {
    #      proxy_pass   http://code-server.operator2018.ru:9914;
    #      proxy_set_header X-Real-IP $remote_addr;
    #      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #      proxy_set_header Host $host;
    #      proxy_set_header X-NginX-Proxy true;
    #      proxy_http_version 1.1;
    #      proxy_set_header Upgrade $http_upgrade;
    #      proxy_set_header Connection "upgrade";
    #      proxy_redirect off;
    #    }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}